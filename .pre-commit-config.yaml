repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks
        entry: gitleaks detect --verbose --config .gitleaks.toml --log-opts=-1
        language: system
        stages: [pre-commit]
        pass_filenames: false
